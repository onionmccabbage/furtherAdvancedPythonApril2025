import sys
import os

print(sys.maxsize)
print(os.getpid())
print(sys.float_info)

# ALL computers are incapable of handling real numbers
# they can only handle than vanishingly small subset of numbers which can be represented in 64 bits